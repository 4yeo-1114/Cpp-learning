# Std Sort Notes

原文件为学习笔记或伪代码，不作为独立编译单元。

```text
//sort(起始迭代器, 结束迭代器, 比较函数);
//排序范围是 [起始，结束)（左闭右开区间），
//即包含起始位置，不包含结束位置
//当数组从 1 开始存储 n 个元素时，
//有效范围是 a[1] 到 a[n]，
//因此结束位置需指定为 a + n + 1

//bool cmp(f A, f B) { return A.d < B.d; }
//接收两个 f 类型的结构体对象 A 和 B 作为参数
//返回一个布尔值，表示排序时 A 应该排在 B 前面还是后面
//当 A.d < B.d 为真时，
//意味着 A 的截止时间小于 B 的截止时间，
//此时 A 应该排在 B 前面


```
